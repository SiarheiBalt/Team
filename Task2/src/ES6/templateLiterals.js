const person = "Alex";
const string = `Hey ${person}`; // Hey Alex

const string1 = `total count is: ${1 + 2 + 3}`;
const string2 = `result is ${foo() ? "x" : "y"}`;

const string3 = `Hey
this

string
is awesome!`;
